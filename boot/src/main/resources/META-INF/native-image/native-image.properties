# GraalVM native-image configuration for Yukta
# Required for JTE (Java Templating Engine) precompiled templates
# Force prod profile for native image builds to ensure:
# - JTE uses precompiled templates (not dynamic compilation)
# - Development-only settings are disabled
Args=-Dspring.profiles.active=prod
