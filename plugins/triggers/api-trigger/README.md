# API Trigger Plugin

A trigger plugin that allows starting a workflow via a REST API call.

## 🏗️ Purpose

The standard entry point for external systems and scripts to initiate a quality gate check in Yukta.
